{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem@2.1.0", "./project"],
      "description": "Official filesystem server, scoped to this repo"
    }
  }
}
